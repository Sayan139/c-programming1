#include <stdio.h>
int main(){
    int n=2,m=4;
    printf("the right shift of the number : %d \n" , n>>1);
    printf("the left shift of the number : %d \n" , n<<1);
    (n>m)?printf("n is greater than m") : printf("m is greater than n");
}